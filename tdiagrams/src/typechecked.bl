Diag(
  SourcePos(Stdin, Pos(1, 1))
, Compile(
    Diag(SourcePos(Stdin, Pos(2, 5)), Program("m", "Haskell"))
  , Diag(
      SourcePos(Stdin, Pos(4, 5))
    , Compile(
        Diag(
          SourcePos(Stdin, Pos(5, 9))
        , Compiler("uuagc", "Haskell", "Haskell", "i686-windows")
        )
      , Diag(
          SourcePos(Stdin, Pos(7, 9))
        , Compiler("uuagc", "i686-windows", "Haskell", "i6")
        )
      )
    )
  )
)
