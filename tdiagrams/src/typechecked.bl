Diag(
  SourcePos(Stdin, Pos(1, 1))
, Compile(
    Diag(
      SourcePos(Stdin, Pos(2, 5))
    , Interpreter("hugs", "Haskell", "Haskell")
    )
  , Diag(
      SourcePos(Stdin, Pos(4, 5))
    , Compile(
        Diag(
          SourcePos(Stdin, Pos(5, 9))
        , Compiler("C2", "Haskell", "Haskell", "i686")
        )
      , Diag(
          SourcePos(Stdin, Pos(7, 9))
        , Compiler("C3", "i686", "Haskell", "i6")
        )
      )
    )
  )
)
